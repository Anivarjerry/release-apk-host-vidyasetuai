-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.users (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  auth_id uuid NOT NULL UNIQUE,
  email text NOT NULL UNIQUE,
  mobile_number text UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT users_pkey PRIMARY KEY (id),
  CONSTRAINT users_auth_id_fkey FOREIGN KEY (auth_id) REFERENCES auth.users(id)
);
CREATE TABLE public.user_profiles (
  user_id uuid NOT NULL,
  username text UNIQUE,
  first_name text,
  last_name text,
  full_name text,
  profile_picture_url text,
  cover_photo_url text,
  gender USER-DEFINED,
  date_of_birth date,
  bio text,
  preferred_language character varying,
  is_private boolean NOT NULL DEFAULT false,
  is_verified boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  CONSTRAINT user_profiles_pkey PRIMARY KEY (user_id),
  CONSTRAINT user_profiles_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.user_sessions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  session_id uuid NOT NULL DEFAULT gen_random_uuid() UNIQUE,
  device_id text NOT NULL,
  device_name text,
  fcm_token text,
  refresh_token_hash text,
  is_active boolean NOT NULL DEFAULT true,
  last_seen_at timestamp with time zone,
  expires_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  platform text NOT NULL,
  CONSTRAINT user_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT user_sessions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.global_boards (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text NOT NULL UNIQUE,
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT global_boards_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_mediums (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  board_id uuid NOT NULL,
  name text NOT NULL,
  code text NOT NULL UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT global_mediums_pkey PRIMARY KEY (id),
  CONSTRAINT global_mediums_board_id_fkey FOREIGN KEY (board_id) REFERENCES public.global_boards(id)
);
CREATE TABLE public.global_classes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text NOT NULL UNIQUE,
  level_order integer NOT NULL,
  education_level text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  is_deleted boolean NOT NULL DEFAULT false,
  CONSTRAINT global_classes_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_subjects (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text UNIQUE,
  subject_type text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT global_subjects_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_exam_types (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text UNIQUE,
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT global_exam_types_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_fee_heads (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text NOT NULL UNIQUE,
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  is_additional boolean NOT NULL DEFAULT false,
  CONSTRAINT global_fee_heads_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_languages (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text NOT NULL UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT global_languages_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_blood_groups (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT global_blood_groups_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_student_categories (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text NOT NULL UNIQUE,
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT global_student_categories_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_relationship_types (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text NOT NULL UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT global_relationship_types_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_attendance_status (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text NOT NULL UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT global_attendance_status_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_student_status (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text NOT NULL UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT global_student_status_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_result_status (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text NOT NULL UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT global_result_status_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_staff_roles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text NOT NULL UNIQUE,
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  CONSTRAINT global_staff_roles_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_organization_types (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text NOT NULL UNIQUE,
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_own boolean NOT NULL DEFAULT true,
  CONSTRAINT global_organization_types_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_marksheet_templates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  board_id uuid NOT NULL,
  class_id uuid NOT NULL,
  name text NOT NULL UNIQUE,
  code text NOT NULL UNIQUE,
  template_json jsonb NOT NULL,
  is_default boolean NOT NULL DEFAULT false,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT global_marksheet_templates_pkey PRIMARY KEY (id),
  CONSTRAINT global_marksheet_templates_board_id_fkey FOREIGN KEY (board_id) REFERENCES public.global_boards(id),
  CONSTRAINT global_marksheet_templates_class_id_fkey FOREIGN KEY (class_id) REFERENCES public.global_classes(id)
);
CREATE TABLE public.app_versions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  platform USER-DEFINED NOT NULL,
  version_number text NOT NULL,
  build_number integer NOT NULL,
  release_type USER-DEFINED NOT NULL DEFAULT 'stable'::release_type,
  file_url text NOT NULL,
  changelog text,
  is_latest boolean NOT NULL DEFAULT false,
  force_update boolean NOT NULL DEFAULT false,
  published_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  is_deleted boolean DEFAULT false,
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT app_versions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_sessions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT global_sessions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.organizations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  type_id uuid,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  parent_organization_id uuid,
  is_own boolean NOT NULL DEFAULT true,
  CONSTRAINT organizations_pkey PRIMARY KEY (id),
  CONSTRAINT organizations_type_id_fkey FOREIGN KEY (type_id) REFERENCES public.global_organization_types(id),
  CONSTRAINT organizations_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id)
);
CREATE TABLE public.organization_users (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  user_id uuid NOT NULL,
  role_id uuid,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_users_pkey PRIMARY KEY (id),
  CONSTRAINT organization_users_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_users_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT organization_users_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.global_staff_roles(id)
);
CREATE TABLE public.organization_session_tokens (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_user_id uuid NOT NULL,
  session_token text NOT NULL UNIQUE,
  refresh_token text NOT NULL UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  expires_at timestamp with time zone NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_session_tokens_pkey PRIMARY KEY (id),
  CONSTRAINT organization_session_tokens_org_user_fkey FOREIGN KEY (organization_user_id) REFERENCES public.organization_users(id)
);
CREATE TABLE public.organization_profiles (
  organization_id uuid NOT NULL,
  logo_url text,
  website_url text,
  email text NOT NULL,
  mobile_number text NOT NULL,
  alternate_mobile_number text,
  registration_number text,
  gst_number text,
  pan_number text,
  address_line1 text NOT NULL,
  address_line2 text,
  city text NOT NULL,
  state text NOT NULL DEFAULT 'Rajasthan'::text,
  pincode text NOT NULL,
  active_session_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_setup_complete boolean NOT NULL DEFAULT false,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  CONSTRAINT organization_profiles_pkey PRIMARY KEY (organization_id),
  CONSTRAINT organization_profiles_org_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_profiles_session_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id)
);
CREATE TABLE public.organization_boards (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  board_id uuid NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_boards_pkey PRIMARY KEY (id),
  CONSTRAINT organization_boards_org_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_boards_board_fkey FOREIGN KEY (board_id) REFERENCES public.global_boards(id),
  CONSTRAINT organization_boards_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_boards_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_mediums (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  medium_id uuid NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_mediums_pkey PRIMARY KEY (id),
  CONSTRAINT organization_mediums_org_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_mediums_medium_fkey FOREIGN KEY (medium_id) REFERENCES public.global_mediums(id),
  CONSTRAINT organization_mediums_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_mediums_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_languages (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  language_id uuid NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_languages_pkey PRIMARY KEY (id),
  CONSTRAINT organization_languages_org_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_languages_lang_fkey FOREIGN KEY (language_id) REFERENCES public.global_languages(id),
  CONSTRAINT organization_languages_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_languages_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_attendance_status (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  attendance_status_id uuid NOT NULL,
  salary_payout_percentage numeric NOT NULL DEFAULT 100.0,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_attendance_status_pkey PRIMARY KEY (id),
  CONSTRAINT organization_attendance_status_org_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_attendance_status_status_fkey FOREIGN KEY (attendance_status_id) REFERENCES public.global_attendance_status(id)
);
CREATE TABLE public.organization_classes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  class_id uuid NOT NULL,
  custom_class_name text,
  next_promotion_class_id uuid,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_classes_pkey PRIMARY KEY (id),
  CONSTRAINT organization_classes_org_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_classes_class_fkey FOREIGN KEY (class_id) REFERENCES public.global_classes(id),
  CONSTRAINT organization_classes_promo_fkey FOREIGN KEY (next_promotion_class_id) REFERENCES public.organization_classes(id),
  CONSTRAINT organization_classes_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_classes_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_sections (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_class_id uuid NOT NULL,
  name text NOT NULL,
  code text NOT NULL,
  room_number text,
  max_capacity integer NOT NULL DEFAULT 40,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  updated_by uuid,
  created_by uuid,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_sections_pkey PRIMARY KEY (id),
  CONSTRAINT organization_sections_class_fkey FOREIGN KEY (organization_class_id) REFERENCES public.organization_classes(id),
  CONSTRAINT organization_sections_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id),
  CONSTRAINT organization_sections_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_session_classes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  session_id uuid NOT NULL,
  organization_class_id uuid NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_session_classes_pkey PRIMARY KEY (id),
  CONSTRAINT org_sess_classes_org_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT org_sess_classes_sess_fkey FOREIGN KEY (session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT org_sess_classes_class_fkey FOREIGN KEY (organization_class_id) REFERENCES public.organization_classes(id),
  CONSTRAINT organization_session_classes_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_session_classes_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_session_sections (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_session_class_id uuid NOT NULL,
  organization_section_id uuid NOT NULL,
  class_teacher_id uuid,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_session_sections_pkey PRIMARY KEY (id),
  CONSTRAINT org_sess_sect_class_fkey FOREIGN KEY (organization_session_class_id) REFERENCES public.organization_session_classes(id),
  CONSTRAINT org_sess_sect_sect_fkey FOREIGN KEY (organization_section_id) REFERENCES public.organization_sections(id),
  CONSTRAINT org_sess_sect_teacher_fkey FOREIGN KEY (class_teacher_id) REFERENCES public.organization_users(id),
  CONSTRAINT organization_session_sections_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_session_sections_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_session_subjects (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_session_class_id uuid NOT NULL,
  subject_id uuid NOT NULL,
  subject_teacher_id uuid,
  custom_subject_name text,
  subject_code text,
  is_elective boolean NOT NULL DEFAULT false,
  weekly_hours numeric,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  is_deleted boolean NOT NULL DEFAULT false,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_session_subjects_pkey PRIMARY KEY (id),
  CONSTRAINT org_sess_subj_class_fkey FOREIGN KEY (organization_session_class_id) REFERENCES public.organization_session_classes(id),
  CONSTRAINT org_sess_subj_subj_fkey FOREIGN KEY (subject_id) REFERENCES public.global_subjects(id),
  CONSTRAINT org_sess_subj_teacher_fkey FOREIGN KEY (subject_teacher_id) REFERENCES public.organization_users(id),
  CONSTRAINT organization_session_subjects_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_session_subjects_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_fee_assignments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  session_id uuid NOT NULL,
  organization_class_id uuid NOT NULL,
  fee_head_id uuid NOT NULL,
  amount numeric NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_fee_assignments_pkey PRIMARY KEY (id),
  CONSTRAINT organization_fee_assignments_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_fee_assignments_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_fee_assignments_organization_class_id_fkey FOREIGN KEY (organization_class_id) REFERENCES public.organization_classes(id),
  CONSTRAINT organization_fee_assignments_fee_head_id_fkey FOREIGN KEY (fee_head_id) REFERENCES public.global_fee_heads(id),
  CONSTRAINT organization_fee_assignments_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_fee_assignments_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.global_countries (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  code text UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT global_countries_pkey PRIMARY KEY (id)
);
CREATE TABLE public.global_states (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  country_id uuid NOT NULL,
  name text NOT NULL,
  code text UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT global_states_pkey PRIMARY KEY (id),
  CONSTRAINT global_states_country_id_fkey FOREIGN KEY (country_id) REFERENCES public.global_countries(id)
);
CREATE TABLE public.global_districts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  state_id uuid NOT NULL,
  name text NOT NULL,
  code text UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT global_districts_pkey PRIMARY KEY (id),
  CONSTRAINT global_districts_state_id_fkey FOREIGN KEY (state_id) REFERENCES public.global_states(id)
);
CREATE TABLE public.global_tehsils (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  state_id uuid NOT NULL,
  district_id uuid NOT NULL,
  name text NOT NULL,
  code text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT global_tehsils_pkey PRIMARY KEY (id),
  CONSTRAINT global_tehsils_state_id_fkey FOREIGN KEY (state_id) REFERENCES public.global_states(id),
  CONSTRAINT global_tehsils_district_id_fkey FOREIGN KEY (district_id) REFERENCES public.global_districts(id)
);
CREATE TABLE public.global_areas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  state_id uuid NOT NULL,
  district_id uuid NOT NULL,
  tehsil_id uuid NOT NULL,
  name text NOT NULL,
  code text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  panchayat_id uuid,
  CONSTRAINT global_areas_pkey PRIMARY KEY (id),
  CONSTRAINT global_areas_state_id_fkey FOREIGN KEY (state_id) REFERENCES public.global_states(id),
  CONSTRAINT global_areas_district_id_fkey FOREIGN KEY (district_id) REFERENCES public.global_districts(id),
  CONSTRAINT global_areas_tehsil_id_fkey FOREIGN KEY (tehsil_id) REFERENCES public.global_tehsils(id),
  CONSTRAINT global_areas_panchayat_id_fkey FOREIGN KEY (panchayat_id) REFERENCES public.global_panchayats(id)
);
CREATE TABLE public.global_panchayats (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  state_id uuid NOT NULL,
  district_id uuid NOT NULL,
  tehsil_id uuid NOT NULL,
  name text NOT NULL,
  code text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT global_panchayats_pkey PRIMARY KEY (id),
  CONSTRAINT global_panchayats_state_id_fkey FOREIGN KEY (state_id) REFERENCES public.global_states(id),
  CONSTRAINT global_panchayats_district_id_fkey FOREIGN KEY (district_id) REFERENCES public.global_districts(id),
  CONSTRAINT global_panchayats_tehsil_id_fkey FOREIGN KEY (tehsil_id) REFERENCES public.global_tehsils(id)
);
CREATE TABLE public.global_organization_parent_types (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text NOT NULL UNIQUE,
  description text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT global_organization_parent_types_pkey PRIMARY KEY (id)
);
CREATE TABLE public.organization_parents (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  type_id uuid NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_parents_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parents_type_id_fkey FOREIGN KEY (type_id) REFERENCES public.global_organization_parent_types(id)
);
CREATE TABLE public.organization_periods (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  session_id uuid NOT NULL,
  name USER-DEFINED NOT NULL,
  start_time time without time zone NOT NULL,
  end_time time without time zone NOT NULL,
  is_break boolean NOT NULL DEFAULT false,
  sort_order integer NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_periods_pkey PRIMARY KEY (id),
  CONSTRAINT organization_periods_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_periods_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_periods_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_periods_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.global_expense_types (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  code text UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  reference_type USER-DEFINED,
  CONSTRAINT global_expense_types_pkey PRIMARY KEY (id)
);
CREATE TABLE public.organization_parent_buses (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  bus_number text NOT NULL UNIQUE,
  bus_name text,
  route_name text,
  max_capacity integer,
  insurance_expiry_date date,
  insurance_image_url text,
  fitness_expiry_date date,
  fitness_image_url text,
  pollution_expiry_date date,
  pollution_image_url text,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_parent_buses_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parent_buses_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parent_buses_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_parent_buses_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_parent_buses_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parent_staff (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  role_id uuid NOT NULL,
  name text NOT NULL,
  mobile_number text NOT NULL,
  email text,
  gender text CHECK (gender = ANY (ARRAY['Male'::text, 'Female'::text, 'Other'::text])),
  date_of_joining date NOT NULL DEFAULT CURRENT_DATE,
  date_of_birth date,
  pan_number text,
  aadhaar_number text,
  license_number text,
  license_expiry_date date,
  address_area_id uuid,
  subject_id uuid,
  bus_id uuid,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_parent_staff_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parent_staff_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parent_staff_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_parent_staff_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.global_staff_roles(id),
  CONSTRAINT organization_parent_staff_address_area_id_fkey FOREIGN KEY (address_area_id) REFERENCES public.global_areas(id),
  CONSTRAINT organization_parent_staff_subject_id_fkey FOREIGN KEY (subject_id) REFERENCES public.global_subjects(id),
  CONSTRAINT organization_parent_staff_bus_id_fkey FOREIGN KEY (bus_id) REFERENCES public.organization_parent_buses(id),
  CONSTRAINT organization_parent_staff_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_parent_staff_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parent_bus_staff_assignments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  bus_id uuid NOT NULL,
  staff_id uuid NOT NULL,
  role_in_bus text NOT NULL CHECK (role_in_bus = ANY (ARRAY['Driver'::text, 'Conductor'::text, 'Helper'::text])),
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_parent_bus_staff_assignments_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parent_bus_staff_assig_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parent_bus_staff_assignment_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_parent_bus_staff_assignments_bus_id_fkey FOREIGN KEY (bus_id) REFERENCES public.organization_parent_buses(id),
  CONSTRAINT organization_parent_bus_staff_assignments_staff_id_fkey FOREIGN KEY (staff_id) REFERENCES public.organization_parent_staff(id),
  CONSTRAINT organization_parent_bus_staff_assignments_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_parent_bus_staff_assignments_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parent_staff_salaries (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  staff_id uuid NOT NULL,
  monthly_salary numeric NOT NULL DEFAULT 0,
  bank_name text,
  bank_account_number text,
  ifsc_code text,
  upi_id text,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_parent_staff_salaries_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parent_staff_salaries_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parent_staff_salaries_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_parent_staff_salaries_staff_id_fkey FOREIGN KEY (staff_id) REFERENCES public.organization_parent_staff(id),
  CONSTRAINT organization_parent_staff_salaries_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_parent_staff_salaries_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parent_staff_leave_quotas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  organization_id uuid,
  active_session_id uuid NOT NULL,
  staff_id uuid NOT NULL,
  total_leaves numeric NOT NULL DEFAULT 12.0,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_parent_staff_leave_quotas_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parent_staff_leave_quo_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parent_staff_leave_quotas_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_parent_staff_leave_quotas_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_parent_staff_leave_quotas_staff_id_fkey FOREIGN KEY (staff_id) REFERENCES public.organization_parent_staff(id),
  CONSTRAINT organization_parent_staff_leave_quotas_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_parent_staff_leave_quotas_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parent_staff_bus_fares (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  staff_id uuid NOT NULL,
  bus_id uuid NOT NULL,
  fare_amount numeric NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_parent_staff_bus_fares_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parent_staff_bus_fares_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parent_staff_bus_fares_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_parent_staff_bus_fares_staff_id_fkey FOREIGN KEY (staff_id) REFERENCES public.organization_parent_staff(id),
  CONSTRAINT organization_parent_staff_bus_fares_bus_id_fkey FOREIGN KEY (bus_id) REFERENCES public.organization_parent_buses(id),
  CONSTRAINT organization_parent_staff_bus_fares_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_parent_staff_bus_fares_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parent_staff_bus_enrollments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  staff_id uuid NOT NULL,
  bus_id uuid NOT NULL,
  joining_date date NOT NULL DEFAULT CURRENT_DATE,
  monthly_fare numeric NOT NULL DEFAULT 0,
  auto_deduct_from_salary boolean NOT NULL DEFAULT true,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_parent_staff_bus_enrollments_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parent_staff_bus_enrol_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parent_staff_bus_enrollments_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_parent_staff_bus_enrollment_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_parent_staff_bus_enrollments_staff_id_fkey FOREIGN KEY (staff_id) REFERENCES public.organization_parent_staff(id),
  CONSTRAINT organization_parent_staff_bus_enrollments_bus_id_fkey FOREIGN KEY (bus_id) REFERENCES public.organization_parent_buses(id),
  CONSTRAINT organization_parent_staff_bus_enrollments_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_parent_staff_bus_enrollments_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parent_staff_attendance (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  staff_id uuid NOT NULL,
  attendance_date date NOT NULL DEFAULT CURRENT_DATE,
  status_id uuid NOT NULL,
  is_paid_leave boolean NOT NULL DEFAULT true,
  check_in_time time without time zone,
  check_out_time time without time zone,
  remarks text,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_parent_staff_attendance_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parent_staff_attendanc_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parent_staff_attendance_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_parent_staff_attendance_staff_id_fkey FOREIGN KEY (staff_id) REFERENCES public.organization_parent_staff(id),
  CONSTRAINT organization_parent_staff_attendance_status_id_fkey FOREIGN KEY (status_id) REFERENCES public.global_attendance_status(id),
  CONSTRAINT organization_parent_staff_attendance_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_parent_staff_attendance_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parent_staff_salary_payouts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  staff_id uuid NOT NULL,
  payout_month integer NOT NULL CHECK (payout_month >= 1 AND payout_month <= 12),
  payout_year integer NOT NULL,
  salary_amount numeric NOT NULL,
  is_locked boolean NOT NULL DEFAULT false,
  locked_at timestamp with time zone,
  locked_by uuid,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  bonus numeric NOT NULL DEFAULT 0.0,
  deduction numeric NOT NULL DEFAULT 0.0,
  CONSTRAINT organization_parent_staff_salary_payouts_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parent_staff_salary_pa_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parent_staff_salary_payouts_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_parent_staff_salary_payouts_staff_id_fkey FOREIGN KEY (staff_id) REFERENCES public.organization_parent_staff(id),
  CONSTRAINT organization_parent_staff_salary_payouts_locked_by_fkey FOREIGN KEY (locked_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parent_staff_salary_payments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  staff_id uuid NOT NULL,
  salary_payout_id uuid NOT NULL,
  payment_date date NOT NULL DEFAULT CURRENT_DATE,
  amount_paid numeric NOT NULL DEFAULT 0,
  payment_mode text NOT NULL CHECK (payment_mode = ANY (ARRAY['Cash'::text, 'Cheque'::text, 'UPI'::text, 'Online'::text])),
  cash_paid_by_user_id uuid,
  cheque_number text,
  cheque_date date,
  cheque_bank_name text,
  online_transaction_id text,
  online_payment_app text,
  remarks text,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_parent_staff_salary_payments_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parent_staff_salary_p_parent_organization_id_fkey1 FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parent_staff_salary_payment_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_parent_staff_salary_payments_staff_id_fkey FOREIGN KEY (staff_id) REFERENCES public.organization_parent_staff(id),
  CONSTRAINT organization_parent_staff_salary_payments_salary_payout_id_fkey FOREIGN KEY (salary_payout_id) REFERENCES public.organization_parent_staff_salary_payouts(id),
  CONSTRAINT organization_parent_staff_salary_paym_cash_paid_by_user_id_fkey FOREIGN KEY (cash_paid_by_user_id) REFERENCES public.users(id),
  CONSTRAINT organization_parent_staff_salary_payments_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_parent_staff_salary_payments_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parent_staff_user_links (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  staff_id uuid NOT NULL,
  user_id uuid NOT NULL,
  is_approved boolean NOT NULL DEFAULT false,
  approved_by uuid,
  approved_at timestamp with time zone,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_parent_staff_user_links_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parent_staff_user_link_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parent_staff_user_links_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_parent_staff_user_links_staff_id_fkey FOREIGN KEY (staff_id) REFERENCES public.organization_parent_staff(id),
  CONSTRAINT organization_parent_staff_user_links_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT organization_parent_staff_user_links_approved_by_fkey FOREIGN KEY (approved_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parent_expenses (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  expense_type_id uuid NOT NULL,
  payment_method text NOT NULL CHECK (payment_method = ANY (ARRAY['Cash'::text, 'Cheque'::text, 'UPI'::text, 'Online'::text])),
  amount numeric NOT NULL DEFAULT 0,
  receipt_uuid uuid,
  admin_note text,
  expense_date date NOT NULL DEFAULT CURRENT_DATE,
  reference_id uuid,
  reference_type text CHECK (reference_type = ANY (ARRAY['Bus'::text, 'ChildOrganization'::text, 'Salary'::text])),
  cash_paid_to text,
  cheque_number text,
  cheque_date date,
  cheque_bank_name text,
  online_transaction_id text,
  online_payment_app text,
  vendor_name text,
  bill_number text,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_parent_expenses_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parent_expenses_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parent_expenses_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_parent_expenses_expense_type_id_fkey FOREIGN KEY (expense_type_id) REFERENCES public.global_expense_types(id),
  CONSTRAINT organization_parent_expenses_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_parent_expenses_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_bus_child_assignments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  bus_id uuid NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_bus_child_assignments_pkey PRIMARY KEY (id),
  CONSTRAINT organization_bus_child_assignments_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_bus_child_assignments_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_bus_child_assignments_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_bus_child_assignments_bus_id_fkey FOREIGN KEY (bus_id) REFERENCES public.organization_parent_buses(id),
  CONSTRAINT organization_bus_child_assignments_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_bus_child_assignments_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_guardians (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  name text NOT NULL,
  mobile_number text NOT NULL,
  relationship_type_id uuid NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_guardians_pkey PRIMARY KEY (id),
  CONSTRAINT organization_guardians_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_guardians_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_guardians_relationship_type_id_fkey FOREIGN KEY (relationship_type_id) REFERENCES public.global_relationship_types(id),
  CONSTRAINT organization_guardians_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_guardians_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_students (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  guardian_id uuid NOT NULL,
  category_id uuid,
  blood_group_id uuid,
  student_status_id uuid,
  name text NOT NULL,
  gender text CHECK (gender = ANY (ARRAY['Male'::text, 'Female'::text, 'Other'::text])),
  sr_number text NOT NULL,
  admission_date date NOT NULL,
  date_of_birth date NOT NULL,
  enrollment_number text UNIQUE,
  address_area_id uuid,
  address_details text,
  image_url text,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_students_pkey PRIMARY KEY (id),
  CONSTRAINT organization_students_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_students_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_students_guardian_id_fkey FOREIGN KEY (guardian_id) REFERENCES public.organization_guardians(id),
  CONSTRAINT organization_students_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.global_student_categories(id),
  CONSTRAINT organization_students_blood_group_id_fkey FOREIGN KEY (blood_group_id) REFERENCES public.global_blood_groups(id),
  CONSTRAINT organization_students_student_status_id_fkey FOREIGN KEY (student_status_id) REFERENCES public.global_student_status(id),
  CONSTRAINT organization_students_address_area_id_fkey FOREIGN KEY (address_area_id) REFERENCES public.global_areas(id),
  CONSTRAINT organization_students_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_students_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_student_enrollments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  student_id uuid NOT NULL,
  class_id uuid NOT NULL,
  section_id uuid NOT NULL,
  result_status_id uuid,
  roll_number integer,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_student_enrollments_pkey PRIMARY KEY (id),
  CONSTRAINT organization_student_enrollments_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_student_enrollments_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_student_enrollments_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.organization_students(id),
  CONSTRAINT organization_student_enrollments_class_id_fkey FOREIGN KEY (class_id) REFERENCES public.organization_classes(id),
  CONSTRAINT organization_student_enrollments_section_id_fkey FOREIGN KEY (section_id) REFERENCES public.organization_sections(id),
  CONSTRAINT organization_student_enrollments_result_status_id_fkey FOREIGN KEY (result_status_id) REFERENCES public.global_result_status(id),
  CONSTRAINT organization_student_enrollments_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_student_enrollments_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_student_bus_assignments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  student_id uuid NOT NULL,
  bus_id uuid NOT NULL,
  pickup_stop text,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_student_bus_assignments_pkey PRIMARY KEY (id),
  CONSTRAINT organization_student_bus_assignments_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_student_bus_assignments_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_student_bus_assignments_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.organization_students(id),
  CONSTRAINT organization_student_bus_assignments_bus_id_fkey FOREIGN KEY (bus_id) REFERENCES public.organization_parent_buses(id),
  CONSTRAINT organization_student_bus_assignments_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_student_bus_assignments_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_student_additional_fees (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  student_id uuid NOT NULL,
  global_fee_head_id uuid NOT NULL,
  amount numeric NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_student_additional_fees_pkey PRIMARY KEY (id),
  CONSTRAINT organization_student_additional_fees_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_student_additional_fees_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_student_additional_fees_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.organization_students(id),
  CONSTRAINT organization_student_additional_fees_global_fee_head_id_fkey FOREIGN KEY (global_fee_head_id) REFERENCES public.global_fee_heads(id),
  CONSTRAINT organization_student_additional_fees_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_student_additional_fees_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_student_fee_payments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  student_id uuid NOT NULL,
  receipt_number text NOT NULL,
  payment_mode text NOT NULL CHECK (payment_mode = ANY (ARRAY['Cash'::text, 'Cheque'::text, 'UPI'::text, 'Online'::text])),
  payment_date date NOT NULL DEFAULT CURRENT_DATE,
  amount_paid numeric NOT NULL DEFAULT 0,
  discount_amount numeric NOT NULL DEFAULT 0,
  fine_amount numeric NOT NULL DEFAULT 0,
  discount_reason text,
  cash_received_by_user_id uuid,
  cheque_number text,
  cheque_date date,
  cheque_bank_name text,
  online_transaction_id text,
  online_payment_app text,
  remarks text,
  status text NOT NULL DEFAULT 'Completed'::text CHECK (status = ANY (ARRAY['Completed'::text, 'Cancelled'::text])),
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_student_fee_payments_pkey PRIMARY KEY (id),
  CONSTRAINT organization_student_fee_payments_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_student_fee_payments_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_student_fee_payments_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.organization_students(id),
  CONSTRAINT organization_student_fee_payments_cash_received_by_user_id_fkey FOREIGN KEY (cash_received_by_user_id) REFERENCES public.users(id),
  CONSTRAINT organization_student_fee_payments_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_student_fee_payments_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_student_user_links (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  student_id uuid NOT NULL,
  user_id uuid NOT NULL,
  is_approved boolean NOT NULL DEFAULT false,
  approved_by uuid,
  approved_at timestamp with time zone,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_student_user_links_pkey PRIMARY KEY (id),
  CONSTRAINT organization_student_user_links_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_student_user_links_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_student_user_links_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.organization_students(id),
  CONSTRAINT organization_student_user_links_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT organization_student_user_links_approved_by_fkey FOREIGN KEY (approved_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_guardian_user_links (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  guardian_id uuid NOT NULL,
  user_id uuid NOT NULL,
  is_approved boolean NOT NULL DEFAULT false,
  approved_by uuid,
  approved_at timestamp with time zone,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_guardian_user_links_pkey PRIMARY KEY (id),
  CONSTRAINT organization_guardian_user_links_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_guardian_user_links_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_guardian_user_links_guardian_id_fkey FOREIGN KEY (guardian_id) REFERENCES public.organization_guardians(id),
  CONSTRAINT organization_guardian_user_links_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT organization_guardian_user_links_approved_by_fkey FOREIGN KEY (approved_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_student_marks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  exam_subject_id uuid NOT NULL,
  student_id uuid NOT NULL,
  class_id uuid NOT NULL,
  marks_obtained numeric,
  is_absent boolean NOT NULL DEFAULT false,
  grade text,
  is_passed boolean,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_student_marks_pkey PRIMARY KEY (id),
  CONSTRAINT organization_student_marks_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_student_marks_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_student_marks_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.organization_students(id),
  CONSTRAINT organization_student_marks_class_id_fkey FOREIGN KEY (class_id) REFERENCES public.organization_classes(id),
  CONSTRAINT organization_student_marks_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_student_marks_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_student_homeworks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  class_id uuid NOT NULL,
  section_id uuid NOT NULL,
  subject_id uuid NOT NULL,
  title text NOT NULL,
  description text,
  attachment_url text,
  submission_date date,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_student_homeworks_pkey PRIMARY KEY (id),
  CONSTRAINT organization_student_homeworks_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_student_homeworks_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_student_homeworks_class_id_fkey FOREIGN KEY (class_id) REFERENCES public.organization_classes(id),
  CONSTRAINT organization_student_homeworks_section_id_fkey FOREIGN KEY (section_id) REFERENCES public.organization_sections(id),
  CONSTRAINT organization_student_homeworks_subject_id_fkey FOREIGN KEY (subject_id) REFERENCES public.global_subjects(id),
  CONSTRAINT organization_student_homeworks_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_student_homeworks_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_inquiries (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  student_name text NOT NULL,
  guardian_name text NOT NULL,
  mobile_number text NOT NULL,
  class_id uuid,
  inquiry_date date NOT NULL DEFAULT CURRENT_DATE,
  status text NOT NULL DEFAULT 'Pending'::text CHECK (status = ANY (ARRAY['Pending'::text, 'Admitted'::text, 'Cancelled'::text])),
  remarks text,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_inquiries_pkey PRIMARY KEY (id),
  CONSTRAINT organization_inquiries_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT organization_inquiries_active_session_id_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT organization_inquiries_class_id_fkey FOREIGN KEY (class_id) REFERENCES public.organization_classes(id),
  CONSTRAINT organization_inquiries_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT organization_inquiries_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parents_users (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  user_id uuid NOT NULL,
  role_id uuid,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_parents_users_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parents_users_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parents_users_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.global_staff_roles(id),
  CONSTRAINT organization_parents_users_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parents_session_tokens (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_user_id uuid NOT NULL,
  session_token text NOT NULL UNIQUE,
  refresh_token text NOT NULL UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  expires_at timestamp with time zone NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_parents_session_tokens_pkey PRIMARY KEY (id),
  CONSTRAINT organization_parents_session_tokens_user_fkey FOREIGN KEY (parent_organization_user_id) REFERENCES public.organization_parents_users(id)
);
CREATE TABLE public.organization_parents_profiles (
  parent_organization_id uuid NOT NULL,
  logo_url text,
  website_url text,
  email text NOT NULL,
  mobile_number text NOT NULL,
  alternate_mobile_number text,
  registration_number text,
  gst_number text,
  pan_number text,
  address_line1 text NOT NULL,
  address_line2 text,
  city text NOT NULL,
  state text NOT NULL DEFAULT 'Rajasthan'::text,
  pincode text NOT NULL,
  active_session_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_setup_complete boolean NOT NULL DEFAULT false,
  CONSTRAINT organization_parents_profiles_pkey PRIMARY KEY (parent_organization_id),
  CONSTRAINT organization_parents_profiles_parent_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT organization_parents_profiles_session_fkey FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id)
);
CREATE TABLE public.global_exam_subject_rules (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  class_id uuid NOT NULL,
  subject_id uuid NOT NULL,
  exam_type_id uuid NOT NULL,
  default_max_marks numeric NOT NULL DEFAULT 100,
  default_passing_marks numeric NOT NULL DEFAULT 33,
  default_grading_system jsonb NOT NULL DEFAULT '{"A": 80, "B": 70, "C": 60, "D": 50, "E": 40, "F": 0, "A+": 90}'::jsonb,
  is_active boolean NOT NULL DEFAULT true,
  CONSTRAINT global_exam_subject_rules_pkey PRIMARY KEY (id),
  CONSTRAINT fk_class FOREIGN KEY (class_id) REFERENCES public.global_classes(id),
  CONSTRAINT fk_subject FOREIGN KEY (subject_id) REFERENCES public.global_subjects(id),
  CONSTRAINT fk_exam_type FOREIGN KEY (exam_type_id) REFERENCES public.global_exam_types(id)
);
CREATE TABLE public.organization_exams (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  exam_type_id uuid NOT NULL,
  name text NOT NULL,
  start_date date,
  end_date date,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_exams_pkey PRIMARY KEY (id),
  CONSTRAINT fk_org FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT fk_session FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT fk_exam_type FOREIGN KEY (exam_type_id) REFERENCES public.global_exam_types(id)
);
CREATE TABLE public.organization_exam_subject_settings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  exam_id uuid NOT NULL,
  class_id uuid NOT NULL,
  subject_id uuid NOT NULL,
  max_marks numeric,
  minimum_passing_marks numeric,
  grading_system jsonb,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT organization_exam_subject_settings_pkey PRIMARY KEY (id)
);
CREATE TABLE public.organization_student_exam_marks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  exam_id uuid NOT NULL,
  class_id uuid NOT NULL,
  subject_id uuid NOT NULL,
  student_id uuid NOT NULL,
  obtained_marks numeric,
  is_absent boolean NOT NULL DEFAULT false,
  is_medical_leave boolean NOT NULL DEFAULT false,
  teacher_remarks text,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  CONSTRAINT organization_student_exam_marks_pkey PRIMARY KEY (id),
  CONSTRAINT fk_student FOREIGN KEY (student_id) REFERENCES public.organization_students(id)
);
CREATE TABLE public.organization_timetable_schedules (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  class_id uuid NOT NULL,
  section_id uuid NOT NULL,
  period_id uuid NOT NULL,
  subject_id uuid NOT NULL,
  teacher_id uuid NOT NULL,
  days_of_week ARRAY NOT NULL,
  start_date date,
  end_date date,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_timetable_schedules_pkey PRIMARY KEY (id),
  CONSTRAINT fk_timetable_parent_org FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT fk_timetable_child_org FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT fk_timetable_session FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT fk_timetable_class FOREIGN KEY (class_id) REFERENCES public.organization_classes(id),
  CONSTRAINT fk_timetable_section FOREIGN KEY (section_id) REFERENCES public.organization_sections(id),
  CONSTRAINT fk_timetable_period FOREIGN KEY (period_id) REFERENCES public.organization_periods(id),
  CONSTRAINT fk_timetable_subject FOREIGN KEY (subject_id) REFERENCES public.global_subjects(id),
  CONSTRAINT fk_timetable_teacher FOREIGN KEY (teacher_id) REFERENCES public.organization_parent_staff(id),
  CONSTRAINT fk_timetable_created_by FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT fk_timetable_updated_by FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_holidays (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  name text NOT NULL,
  start_date date NOT NULL,
  end_date date NOT NULL,
  description text,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_holidays_pkey PRIMARY KEY (id),
  CONSTRAINT fk_holidays_parent_org FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT fk_holidays_child_org FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT fk_holidays_session FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT fk_holidays_created_by FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT fk_holidays_updated_by FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_leaves (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  organization_id uuid,
  active_session_id uuid NOT NULL,
  applicant_type text NOT NULL CHECK (applicant_type = ANY (ARRAY['staff'::text, 'student'::text])),
  staff_id uuid,
  student_id uuid,
  leave_type text NOT NULL,
  start_date date NOT NULL,
  end_date date NOT NULL,
  is_half_day boolean NOT NULL DEFAULT false,
  half_day_period text CHECK (half_day_period = ANY (ARRAY['First Half'::text, 'Second Half'::text])),
  reason text,
  status text NOT NULL DEFAULT 'Pending'::text CHECK (status = ANY (ARRAY['Pending'::text, 'Approved'::text, 'Rejected'::text])),
  action_remarks text,
  action_by uuid,
  action_at timestamp with time zone,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_leaves_pkey PRIMARY KEY (id),
  CONSTRAINT fk_leaves_parent_org FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT fk_leaves_child_org FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT fk_leaves_session FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT fk_leaves_staff FOREIGN KEY (staff_id) REFERENCES public.organization_parent_staff(id),
  CONSTRAINT fk_leaves_student FOREIGN KEY (student_id) REFERENCES public.organization_students(id),
  CONSTRAINT fk_leaves_action_by FOREIGN KEY (action_by) REFERENCES public.users(id),
  CONSTRAINT fk_leaves_created_by FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT fk_leaves_updated_by FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_student_attendance (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  active_session_id uuid NOT NULL,
  student_id uuid NOT NULL,
  attendance_date date NOT NULL DEFAULT CURRENT_DATE,
  status text NOT NULL CHECK (status = ANY (ARRAY['Present'::text, 'Absent'::text, 'Late'::text, 'Half Day'::text, 'On Leave'::text])),
  remarks text,
  marked_by_staff_id uuid,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_student_attendance_pkey PRIMARY KEY (id),
  CONSTRAINT fk_student_attendance_org FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT fk_student_attendance_session FOREIGN KEY (active_session_id) REFERENCES public.global_sessions(id),
  CONSTRAINT fk_student_attendance_student FOREIGN KEY (student_id) REFERENCES public.organization_students(id),
  CONSTRAINT fk_student_attendance_staff FOREIGN KEY (marked_by_staff_id) REFERENCES public.organization_parent_staff(id),
  CONSTRAINT fk_student_attendance_created_by FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT fk_student_attendance_updated_by FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.content_contributor_verifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  contributor_type text NOT NULL CHECK (contributor_type = ANY (ARRAY['user'::text, 'organization'::text, 'parent_organization'::text])),
  user_id uuid UNIQUE,
  organization_id uuid UNIQUE,
  parent_organization_id uuid UNIQUE,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'approved'::text, 'rejected'::text, 'suspended'::text])),
  applicant_note text,
  reviewed_by uuid,
  reviewed_at timestamp with time zone,
  rejection_reason text,
  suspension_reason text,
  applied_at timestamp with time zone NOT NULL DEFAULT now(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT content_contributor_verifications_pkey PRIMARY KEY (id),
  CONSTRAINT ccv_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT ccv_organization_id_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT ccv_parent_organization_id_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT ccv_reviewed_by_fkey FOREIGN KEY (reviewed_by) REFERENCES public.users(id)
);
CREATE TABLE public.case_studies (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  title text NOT NULL,
  slug text NOT NULL UNIQUE,
  cover_image_url text NOT NULL,
  short_description text NOT NULL,
  language text NOT NULL DEFAULT 'hindi'::text CHECK (language = ANY (ARRAY['hindi'::text, 'english'::text, 'bilingual'::text])),
  tags ARRAY NOT NULL DEFAULT '{}'::text[],
  read_time_minutes integer,
  schema_version integer NOT NULL DEFAULT 1,
  content_blocks jsonb NOT NULL DEFAULT '{"blocks": [], "schema_version": 1}'::jsonb,
  additional_image_urls ARRAY NOT NULL DEFAULT '{}'::text[] CHECK (array_length(additional_image_urls, 1) IS NULL OR array_length(additional_image_urls, 1) <= 4),
  author_type text NOT NULL DEFAULT 'platform'::text CHECK (author_type = ANY (ARRAY['platform'::text, 'user'::text, 'organization'::text, 'parent_organization'::text])),
  author_user_id uuid,
  author_organization_id uuid,
  author_parent_organization_id uuid,
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'under_review'::text, 'published'::text, 'archived'::text, 'rejected'::text])),
  visibility text NOT NULL DEFAULT 'public'::text CHECK (visibility = ANY (ARRAY['public'::text, 'institution_only'::text])),
  published_at timestamp with time zone,
  scheduled_publish_at timestamp with time zone,
  moderated_by uuid,
  moderated_at timestamp with time zone,
  moderation_note text,
  view_count integer NOT NULL DEFAULT 0,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT case_studies_pkey PRIMARY KEY (id),
  CONSTRAINT cs_author_user_id_fkey FOREIGN KEY (author_user_id) REFERENCES public.users(id),
  CONSTRAINT cs_author_organization_id_fkey FOREIGN KEY (author_organization_id) REFERENCES public.organizations(id),
  CONSTRAINT cs_author_parent_organization_id_fkey FOREIGN KEY (author_parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT cs_moderated_by_fkey FOREIGN KEY (moderated_by) REFERENCES public.users(id)
);
CREATE TABLE public.case_study_reactions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  case_study_id uuid NOT NULL,
  user_id uuid NOT NULL,
  reaction_type text NOT NULL DEFAULT 'helpful'::text CHECK (reaction_type = ANY (ARRAY['helpful'::text, 'insightful'::text, 'inspiring'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT case_study_reactions_pkey PRIMARY KEY (id),
  CONSTRAINT csr_case_study_id_fkey FOREIGN KEY (case_study_id) REFERENCES public.case_studies(id),
  CONSTRAINT csr_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.case_study_bookmarks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  case_study_id uuid NOT NULL,
  user_id uuid NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT case_study_bookmarks_pkey PRIMARY KEY (id),
  CONSTRAINT csb_case_study_id_fkey FOREIGN KEY (case_study_id) REFERENCES public.case_studies(id),
  CONSTRAINT csb_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.global_journey_templates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  title text NOT NULL,
  description text,
  duration_days integer NOT NULL,
  category text,
  version integer NOT NULL DEFAULT 1,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT global_journey_templates_pkey PRIMARY KEY (id),
  CONSTRAINT global_journey_templates_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT global_journey_templates_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_journey_templates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  organization_id uuid NOT NULL,
  title text NOT NULL,
  description text,
  duration_days integer NOT NULL,
  category text,
  version integer NOT NULL DEFAULT 1,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_journey_templates_pkey PRIMARY KEY (id),
  CONSTRAINT org_journey_templates_org_fkey FOREIGN KEY (organization_id) REFERENCES public.organizations(id),
  CONSTRAINT org_journey_templates_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT org_journey_templates_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.organization_parent_journey_templates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  parent_organization_id uuid NOT NULL,
  title text NOT NULL,
  description text,
  duration_days integer NOT NULL,
  category text,
  version integer NOT NULL DEFAULT 1,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_parent_journey_templates_pkey PRIMARY KEY (id),
  CONSTRAINT parent_org_journey_templates_parent_org_fkey FOREIGN KEY (parent_organization_id) REFERENCES public.organization_parents(id),
  CONSTRAINT parent_org_journey_templates_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id),
  CONSTRAINT parent_org_journey_templates_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.users(id)
);
CREATE TABLE public.global_journey_tasks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  template_id uuid NOT NULL,
  day_number integer NOT NULL,
  task_title text NOT NULL,
  task_description text,
  task_type text NOT NULL DEFAULT 'read'::text,
  sort_order integer NOT NULL DEFAULT 1,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT global_journey_tasks_pkey PRIMARY KEY (id),
  CONSTRAINT global_journey_tasks_template_fkey FOREIGN KEY (template_id) REFERENCES public.global_journey_templates(id)
);
CREATE TABLE public.organization_journey_tasks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  template_id uuid NOT NULL,
  day_number integer NOT NULL,
  task_title text NOT NULL,
  task_description text,
  task_type text NOT NULL DEFAULT 'read'::text,
  sort_order integer NOT NULL DEFAULT 1,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_journey_tasks_pkey PRIMARY KEY (id),
  CONSTRAINT org_journey_tasks_template_fkey FOREIGN KEY (template_id) REFERENCES public.organization_journey_templates(id)
);
CREATE TABLE public.organization_parent_journey_tasks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  template_id uuid NOT NULL,
  day_number integer NOT NULL,
  task_title text NOT NULL,
  task_description text,
  task_type text NOT NULL DEFAULT 'read'::text,
  sort_order integer NOT NULL DEFAULT 1,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_parent_journey_tasks_pkey PRIMARY KEY (id),
  CONSTRAINT parent_org_journey_tasks_template_fkey FOREIGN KEY (template_id) REFERENCES public.organization_parent_journey_templates(id)
);
CREATE TABLE public.global_journey_mcqs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  template_id uuid NOT NULL,
  day_number integer NOT NULL,
  question_text text NOT NULL,
  option_a text NOT NULL,
  option_b text NOT NULL,
  option_c text NOT NULL,
  option_d text NOT NULL,
  correct_option character NOT NULL,
  explanation text,
  difficulty_level text NOT NULL DEFAULT 'medium'::text,
  points integer NOT NULL DEFAULT 1,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT global_journey_mcqs_pkey PRIMARY KEY (id),
  CONSTRAINT global_journey_mcqs_template_fkey FOREIGN KEY (template_id) REFERENCES public.global_journey_templates(id)
);
CREATE TABLE public.organization_journey_mcqs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  template_id uuid NOT NULL,
  day_number integer NOT NULL,
  question_text text NOT NULL,
  option_a text NOT NULL,
  option_b text NOT NULL,
  option_c text NOT NULL,
  option_d text NOT NULL,
  correct_option character NOT NULL,
  explanation text,
  difficulty_level text NOT NULL DEFAULT 'medium'::text,
  points integer NOT NULL DEFAULT 1,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_journey_mcqs_pkey PRIMARY KEY (id),
  CONSTRAINT org_journey_mcqs_template_fkey FOREIGN KEY (template_id) REFERENCES public.organization_journey_templates(id)
);
CREATE TABLE public.organization_parent_journey_mcqs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  template_id uuid NOT NULL,
  day_number integer NOT NULL,
  question_text text NOT NULL,
  option_a text NOT NULL,
  option_b text NOT NULL,
  option_c text NOT NULL,
  option_d text NOT NULL,
  correct_option character NOT NULL,
  explanation text,
  difficulty_level text NOT NULL DEFAULT 'medium'::text,
  points integer NOT NULL DEFAULT 1,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  created_by uuid,
  updated_by uuid,
  CONSTRAINT organization_parent_journey_mcqs_pkey PRIMARY KEY (id),
  CONSTRAINT parent_org_journey_mcqs_template_fkey FOREIGN KEY (template_id) REFERENCES public.organization_parent_journey_templates(id)
);
CREATE TABLE public.user_journeys (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  global_template_id uuid,
  organization_template_id uuid,
  organization_parent_template_id uuid,
  start_date date NOT NULL DEFAULT CURRENT_DATE,
  current_day integer NOT NULL DEFAULT 1,
  notification_time time without time zone,
  status text NOT NULL DEFAULT 'active'::text,
  total_score integer NOT NULL DEFAULT 0,
  current_streak integer NOT NULL DEFAULT 0,
  best_streak integer NOT NULL DEFAULT 0,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT user_journeys_pkey PRIMARY KEY (id),
  CONSTRAINT user_journeys_user_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT user_journeys_global_temp_fkey FOREIGN KEY (global_template_id) REFERENCES public.global_journey_templates(id),
  CONSTRAINT user_journeys_org_temp_fkey FOREIGN KEY (organization_template_id) REFERENCES public.organization_journey_templates(id),
  CONSTRAINT user_journeys_parent_org_temp_fkey FOREIGN KEY (organization_parent_template_id) REFERENCES public.organization_parent_journey_templates(id)
);
CREATE TABLE public.user_journey_task_progress (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_journey_id uuid NOT NULL,
  day_number integer NOT NULL,
  task_id uuid NOT NULL,
  is_completed boolean NOT NULL DEFAULT false,
  completed_at timestamp with time zone,
  is_synced boolean NOT NULL DEFAULT false,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_active boolean NOT NULL DEFAULT true,
  CONSTRAINT user_journey_task_progress_pkey PRIMARY KEY (id),
  CONSTRAINT user_journey_task_progress_journey_fkey FOREIGN KEY (user_journey_id) REFERENCES public.user_journeys(id)
);
CREATE TABLE public.user_journey_mcq_progress (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_journey_id uuid NOT NULL,
  day_number integer NOT NULL,
  mcq_id uuid NOT NULL,
  selected_option character NOT NULL,
  is_correct boolean NOT NULL,
  score_earned integer NOT NULL DEFAULT 0,
  answered_at timestamp with time zone NOT NULL DEFAULT now(),
  is_synced boolean NOT NULL DEFAULT false,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  is_active boolean NOT NULL DEFAULT true,
  CONSTRAINT user_journey_mcq_progress_pkey PRIMARY KEY (id),
  CONSTRAINT user_journey_mcq_progress_journey_fkey FOREIGN KEY (user_journey_id) REFERENCES public.user_journeys(id)
);
CREATE TABLE public.experiences (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  title text NOT NULL,
  cover_image_url text,
  description text NOT NULL,
  author_user_id uuid NOT NULL,
  inspired_count integer NOT NULL DEFAULT 0,
  status text NOT NULL DEFAULT 'published'::text CHECK (status = ANY (ARRAY['published'::text, 'draft'::text, 'archived'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT experiences_pkey PRIMARY KEY (id),
  CONSTRAINT experiences_author_user_id_fkey FOREIGN KEY (author_user_id) REFERENCES public.users(id)
);
CREATE TABLE public.experience_inspirations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  experience_id uuid NOT NULL,
  user_id uuid NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT experience_inspirations_pkey PRIMARY KEY (id),
  CONSTRAINT exp_insp_experience_id_fkey FOREIGN KEY (experience_id) REFERENCES public.experiences(id),
  CONSTRAINT exp_insp_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.user_inspirations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  inspired_user_id uuid NOT NULL,
  inspiring_user_id uuid NOT NULL,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT user_inspirations_pkey PRIMARY KEY (id),
  CONSTRAINT user_inspirations_inspired_user_id_fkey FOREIGN KEY (inspired_user_id) REFERENCES public.users(id),
  CONSTRAINT user_inspirations_inspiring_user_id_fkey FOREIGN KEY (inspiring_user_id) REFERENCES public.users(id)
);
CREATE TABLE public.rooms (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL,
  category text,
  description text,
  message_cooldown_seconds integer NOT NULL DEFAULT 5,
  is_active boolean NOT NULL DEFAULT true,
  is_deleted boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT rooms_pkey PRIMARY KEY (id)
);